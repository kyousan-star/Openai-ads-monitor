name: OpenAI Ads 亚马逊卖家周报

on:
  schedule:
    - cron: '30 3 * * 1'  # 每周日 23:30 ET = 周一 03:30 UTC
  workflow_dispatch:

jobs:
  weekly-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 搜索并推送飞书周报
        run: python3 report.py
